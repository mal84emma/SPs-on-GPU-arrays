# Model settings for test experiments

results_dir: [results, test]

solver_settings:
  verbose: False
  threads: 1 # no. Gurobi threads

probability_settings:
  n_prior_samples: 3
  n_posterior_samples: 3